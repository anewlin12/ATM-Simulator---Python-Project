#imports the ATMGUI class from the atm_gui script
from atm_gui import ATMGUI


#this creates a new object called atm from the ATMGUI class, it is then ran after using atm.run()
atm = ATMGUI()
atm.run()